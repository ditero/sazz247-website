// import Vue from 'vue';
import { createRouter, createWebHistory } from 'vue-router'; //createRouter, createWebHistory, 
// import HeaderSection from '@/components/HeaderSection.vue';
// import ServicesSection from '@/components/ServicesSection.vue';
// import AboutSection from '@/components/AboutSection.vue';
// import ContactSection from '@/components/ContactSection.vue';
// import FooterSection from '@/components/FooterSection.vue';

// Vue.use(VueRouter);

// const routes = [
//   { path: '/', component: HeaderSection },
// //   { path: '/services', component: ServicesSection },
// //   { path: '/about', component: AboutSection },
// //   { path: '/contact', component: ContactSection },
// //   { path: '/footer', component: FooterSection }
// ];

const router = createRouter({
    history: createWebHistory(),
    routes: [
        { }
    ]
  });

export default router;
